# 手动测试清单

> 适用：QA 测试 · 产品验收 · 开发自测
> 版本：Phase 1E | 目标浏览器：Chrome / Firefox / Safari / Edge 最新版

---

## 测试环境准备

```bash
# 本地启动
cd ~/projects/aftergift-prototype
python3 -m http.server 8080
# 访问 http://localhost:8080

# 或访问线上草稿
# https://conanxin.github.io/drafts/aftergift-prototype/
```

---

## 1. 页面加载

| 测试步骤 | 预期结果 | 当前状态 |
|---------|---------|---------|
| 清除浏览器缓存，打开 `http://localhost:8080/` | 页面正常加载，Hero 首屏完整显示 | ✅ |
| 检查控制台无 JS Error | 无红色报错 | ✅ |
| Hero 标题「后来礼物」显示正确 | 标题正确渲染 | ✅ |
| 双按钮「发布一件礼物」「浏览礼物故事」可见 | 按钮正确显示 | ✅ |

---

## 2. gifts.json 加载

| 测试步骤 | 预期结果 | 当前状态 |
|---------|---------|---------|
| 页面加载后礼物卡片自动出现 | 显示礼物网格（初始 8 条）| ✅ |
| 卡片内容包含：名称、类型、关系、情绪标签、处理方式、价格 | 所有字段正确显示 | ✅ |
| 打开浏览器 DevTools → Network，确认 `gifts.json` HTTP 200 | 状态码 200 | ✅ |
| JSON 无解析错误 | 控制台无 JSON parse error | ✅ |

---

## 3. 筛选功能

| 测试步骤 | 预期结果 | 当前状态 |
|---------|---------|---------|
| 点击「出售」筛选标签 | 仅显示 `action=sell` 的礼物 | ✅ |
| 点击「交换」筛选标签 | 仅显示 `action=exchange` 的礼物 | ✅ |
| 点击「全部」恢复完整列表 | 显示全部礼物（已展示部分）| ✅ |
| 切换筛选后「阅读更多」计数重置 | 从 8 条重新开始 | ✅ |
| 切换筛选时控制台无报错 | 无 JS Error | ✅ |

---

## 4. 已收藏筛选

| 测试步骤 | 预期结果 | 当前状态 |
|---------|---------|---------|
| 点击「已收藏」标签 | 若无收藏显示空状态文案 | ✅ |
| 收藏任意礼物后，再点「已收藏」 | 显示已收藏的卡片 | ✅ |
| 取消收藏后点「已收藏」 | 卡片消失或显示空状态 | ✅ |
| 刷新页面后「已收藏」筛选状态 | 收藏状态保留，心形按钮正确显示 | ✅ |

---

## 5. 礼物详情 Modal

| 测试步骤 | 预期结果 | 当前状态 |
|---------|---------|---------|
| 点击任意礼物卡片 | Modal 打开，显示完整故事 | ✅ |
| Modal 包含：礼物名称、类型、关系、情绪、处理方式、价格、完整故事 | 所有字段正确显示 | ✅ |
| Modal 安全提示正确（根据 action 显示对应文案）| 安全提示文案准确 | ✅ |
| 点击右上角 X 关闭 | Modal 关闭，页面滚动恢复正常 | ✅ |
| 按 Escape 键关闭 | Modal 关闭 | ✅ |
| 移动端 Modal 从底部滑出 | 移动端交互正常 | ✅ |

---

## 6. 收藏持久化

| 测试步骤 | 预期结果 | 当前状态 |
|---------|---------|---------|
| 点击卡片右上角心形按钮 | 按钮变为实心珊瑚色，显示「已收藏」Toast | ✅ |
| 刷新页面（不关闭标签页）| 收藏状态保留 | ✅ |
| 打开新标签页访问同一 URL | 收藏状态保留（localStorage 同源）| ✅ |
| 点击 Modal 内「收藏故事」按钮 | 同上效果 | ✅ |
| 再次点击心形取消收藏 | 按钮恢复空心，显示「已取消收藏」Toast | ✅ |

---

## 7. 阅读更多

| 测试步骤 | 预期结果 | 当前状态 |
|---------|---------|---------|
| 初始状态「阅读更多」按钮显示剩余数量 | 如「阅读更多故事（还剩 6 件）」| ✅ |
| 点击「阅读更多」 | 卡片数量增加 8 条 | ✅ |
| 持续点击直到全部加载完毕 | 按钮消失 | ✅ |
| 切换筛选后重置已展示数量 | 按钮重新出现 | ✅ |

---

## 8. 发布表单

| 测试步骤 | 预期结果 | 当前状态 |
|---------|---------|---------|
| 点击「发布一件礼物」滚动到发布区 | 页面平滑滚动到发布表单 | ✅ |
| 填写必填字段（名称、类型、处理方式、一句话故事）| 字段正常输入 | ✅ |
| 选择情绪标签 | 标签高亮，隐藏字段更新 | ✅ |
| 填写价格/交换意向 | 正常显示 | ✅ |
| 点击「发布礼物」（不勾选确认）| Toast 提示「请先确认这个故事适合被公开阅读」| ✅ |
| 勾选 3 项确认，点击发布 | 新卡片出现在列表顶部，表单重置 | ✅ |

---

## 9. 故事预检（Phase 1C 兼容）

| 测试步骤 | 预期结果 | 当前状态 |
|---------|---------|---------|
| 填写一句话故事（如包含手机号），点击「检查故事是否适合公开」| 显示风险卡片（手机号匹配）| ✅ |
| 填写无风险内容，点击「检查故事」| 显示「适合公开」| ✅ |
| 不勾选确认直接提交 | Toast 提示需要确认 | ✅ |

---

## 10. AI 编辑建议（Phase 1D）

| 测试步骤 | 预期结果 | 当前状态 |
|---------|---------|---------|
| 填写含手机号的完整故事，点击「运行温柔检查」| AI Review Panel 展开，显示「身份信息风险」| ✅ |
| 填写含报复词汇的故事，点击「运行温柔检查」| 显示「报复/攻击性表达」| ✅ |
| 填写符合规范的故事，点击「运行温柔检查」| 显示「适合公开」| ✅ |
| 有匿名化建议时显示建议卡片 | 每条含：类型标签 / 原表达 / 原因 / 建议改法 | ✅ |
| 填写短故事（< 50 字）点击检查 | 显示「故事完整度」建议 | ✅ |
| 有匿名化建议时出现「复制匿名化建议」按钮 | 点击后 Toast「已复制」| ✅ |

---

## 11. 复制匿名化建议

| 测试步骤 | 预期结果 | 当前状态 |
|---------|---------|---------|
| 填写含违规信息的故事，运行温柔检查 | 显示建议列表 | ✅ |
| 点击「复制匿名化建议」| Toast「已复制到剪贴板」| ✅ |
| 粘贴到文本编辑器 | 内容为所有建议的原文→改法格式 | ✅ |

---

## 12. 预览发布效果

| 测试步骤 | 预期结果 | 当前状态 |
|---------|---------|---------|
| 填写必填字段，点击「预览发布效果」| Modal 打开，显示预览卡片样式 | ✅ |
| 预览顶部有温柔提示 Banner | 「这是别人将看到的样子」| ✅ |
| 预览内容与表单数据一致 | 名称/类型/价格/故事正确 | ✅ |
| 点击「关闭预览」| Modal 关闭 | ✅ |

---

## 13. 发布前确认 Checkbox

| 测试步骤 | 预期结果 | 当前状态 |
|---------|---------|---------|
| 不勾选任何确认，点击发布 | Toast「请先确认这个故事适合被公开阅读」| ✅ |
| 只勾选 1 项，点击发布 | 同上 Toast | ✅ |
| 勾选全部 3 项，点击发布 | 发布成功，新卡片出现 | ✅ |
| 发布成功后确认框状态 | 3 项全部取消勾选 | ✅ |

---

## 14. 空状态

| 测试步骤 | 预期结果 | 当前状态 |
|---------|---------|---------|
| 切换到无结果的筛选（如「捐出」但暂无）| 显示空状态文案 + CSS 信封图标 | ✅ |
| 点击「已收藏」（无收藏时）| 显示「你还没有收藏任何故事……」| ✅ |
| 空状态文案语气温柔 | 非指责语气 | ✅ |

---

## 15. 移动端断点

| 测试步骤 | 预期结果 | 当前状态 |
|---------|---------|---------|
| Chrome DevTools → iPhone 12（390px）| 单列布局，无横向滚动 | ✅ |
| 360px（极窄屏）| 文字不溢出，按钮正常显示 | ✅ |
| 发布表单按钮全宽 | 预检/预览/提交按钮全宽 | ✅ |
| Modal 从底部滑出 | 移动端 Modal 交互正确 | ✅ |
| 情绪标签不溢出 | 8 个标签在两行内显示正常 | ✅ |
| Checkbox 标签正常换行 | 不重叠、不截断 | ✅ |

---

## 16. noindex 检查

| 测试步骤 | 预期结果 | 当前状态 |
|---------|---------|---------|
| `grep -i "noindex" index.html`（源目录）| 存在 `<meta name="robots" content="noindex, nofollow">` | ✅ |
| `grep -i "noindex" ~/conanxin.github.io/drafts/aftergift-prototype/index.html`（Pages 副本）| 同上 | ✅ |
| 线上 `curl` 检查 | HTML 源码包含 `noindex` | ✅ |

---

## 17. 无 CDN 检查

| 测试步骤 | 预期结果 | 当前状态 |
|---------|---------|---------|
| `grep -r "https://\|http://" index.html style.css app.js`（排除 data-uri）| 仅 SVG namespace + data-uri SVG，无 CDN | ✅ |
| DevTools → Network → JS/CSS | 所有资源来自 localhost 或同域 | ✅ |

---

## 18. 根目录 gifts.json 不存在

| 测试步骤 | 预期结果 | 当前状态 |
|---------|---------|---------|
| `ls ~/conanxin.github.io/drafts/aftergift-prototype/gifts.json` | 文件不存在（Phase 1C.2 已清理）| ✅ |
| Pages 线上访问 `https://conanxin.github.io/drafts/aftergift-prototype/gifts.json` | HTTP 404（git tree 确认已删除）| ✅ |

---

## 附录：问题记录表

| 编号 | 发现日期 | 问题描述 | 严重性 | 状态 | 备注 |
|------|---------|---------|-------|------|------|
| — | — | 无已知问题 | — | — | — |

---

## 签名

| 角色 | 姓名 | 日期 |
|------|------|------|
| 测试执行 | | |
| 产品验收 | | |
